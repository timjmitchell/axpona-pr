# @generated by /reference-commit on 2026-04-07T00:00:00Z, target: AXPONA (JD Events LLC)
#
# This file marks the 3P/1P boundary for this engagement.
# Everything in outside-in/ produced before this date is 3P market baseline.
# Everything produced after (by /synthesize, /derive, etc.) is 1P engagement-specific.

engagement:
  target: "AXPONA (JD Events LLC)"
  mode: consulting
  declared_on: "2026-04-07T00:00:00Z"

  context: "context.md"

  baseline:
    classification: "classification.yaml"
    reference_model: "reference-model.yaml"
    catalogs:
      - event-marketplace
      - ad-supported-media
      - one-time-project
      - marketplace
    decompositions: []
    patterns: []

  competitive_set: []
    # No competitors analyzed at full depth. AXPONA is a niche monopoly —
    # no direct competitor at scale exists in the consumer audio trade show
    # space. Regional audio shows (RMAF, Florida Audio Expo, Capital
    # Audiofest) exist but were not decomposed.

  baseline_assessment:
    classification: present
    reference_model: present
    catalogs_matched: 3
    decompositions_completed: 0
    cross_domain_signals: 3
    completeness: thin

  notes: >
    Baseline is catalog-only — no vendor decompositions in the event/trade
    show space exist in research-pr. This is acceptable because: (1) AXPONA
    is a niche monopoly with no direct competitor worth decomposing at full
    depth, (2) the event-marketplace catalog was originally derived FROM the
    AXPONA engagement and is comprehensive, (3) extensive 1P data exists
    (53 bronze crawls, 4 staging extractions, validated DDD model with 13/13
    rules passing, purchase due diligence documents). The 1P richness
    compensates for the thin 3P competitive set. Vendor decompositions of
    event tech platforms (Cvent, Bizzabo, Freeman) could strengthen the
    baseline but are not blocking for synthesis.
