# @generated by /derive-manifests on 2026-04-07T00:00:00Z, target: AXPONA (JD Events LLC)
#
# AXPONA — System Manifest (top-level)
# Ties all data layers together.

system:
  name: axpona
  display_name: "AXPONA (JD Events LLC)"
  industry: trade_shows
  business_model: "event-marketplace (marketplace 0.60, advertising 0.25, project 0.15)"
  engagement: "axpona-pr#3"
  mirror_state: predicted
  deployment_mode: mode-1  # single-repo consulting deployment

layers:
  domain: "domain/manifest.yaml"
  bronze: "bronze/manifest.yaml"
  silver: "silver/manifest.yaml"
  gold: "gold/manifest.yaml"
  governance: "governance/manifest.yaml"

bounded_contexts:
  core: [exhibitor, sponsorship, exhibition-space, event]
  supporting: [attendee, interaction]
  generic: [agreement, finance]

systems:
  application:
    - Unity Event Solutions (attendee — confirmed)
    - Badge Scanning System (interaction — unconfirmed)
    - WordPress (event/exhibitor website — confirmed)
  record:
    - QuickBooks / Equivalent (finance — unconfirmed)
    - DocuSign / PandaDoc (agreement — unconfirmed)
  work:
    - CRM / Spreadsheet (exhibitor, sponsorship — unconfirmed)
    - Floor Plan Tool (exhibition-space — unconfirmed)
    - Internal management (event — unconfirmed)
  analytics: []  # No analytics systems at current scale

schema:
  path: "axpona-schema.sql"
  tables: 26
  indexes: 36
  views: 8
  status: "hand-built, validated (13/13 derivation rules pass)"
