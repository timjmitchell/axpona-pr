# @generated by /scale-project on 2026-04-07T00:00:00Z, target: axpona-pr
# Capability: all domain model operations (26 tables, 36 indexes, 8 views)
# Vector: data-volume (trivially small — decades of editions < 1M rows)
# Gap: Clean — single-node PostgreSQL handles this forever
#
# Chain: domain-model → all-capabilities → StatefulSet
# The domain model IS the system of record. No vendor owns the data.
# axpona-schema.sql deploys directly as the init script.
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: axpona
spec:
  serviceName: postgres
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: axpona
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: password
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
            - name: init-schema
              mountPath: /docker-entrypoint-initdb.d
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: init-schema
          configMap:
            name: axpona-schema
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 5Gi   # decades of editions fit in 5Gi
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: axpona
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
  clusterIP: None
