# AXPONA — Environment Variables
# Copy to .env and fill in required values

# PostgreSQL
POSTGRES_USER=axpona
POSTGRES_PASSWORD=axpona_dev

# Anthropic API (required for agent runtime and pipeline)
ANTHROPIC_API_KEY=
